# 남은 작업 — 2026-09-01 기준

> 이 문서 → `CLAUDE.md` → 마스터 로드맵(`2026-08-26-nextjs-rearchitecture.md`) 순으로 읽는다.
> superpowers TDD, 커밋은 한 명령에 하나씩(훅이 전체 스위트를 돌린다).

기준 상태: 활성 기업 56 · 분석 실행 183 · 확정 기록 48 · 재무 확보 13 · 미확인 경보·주의 26 ·
테스트 155파일 1,164건 통과 · `tsc` 0 · 린트 0 error(14 warning, 기존).

---

## 1. 코드로 지금 할 수 있는 것

### 1-1. 재무 결측 대리지표 (가장 값이 큰 잔여 작업)

56개사 중 재무를 확보한 것은 **13개사**뿐이다. 나머지 43개사는 DART 정기보고서도 감사보고서도 없다.
API 활용신청은 **이미 완료**돼 있으므로 구현만 남았다. 상세 스펙은 `CLAUDE.md` 의 "확장 예정" 표.

| 원천 | 얻을 것 | 함정 (실측으로 확인된 것) |
|---|---|---|
| 나라장터 **낙찰** (`as/ScsbidInfoService`) | 공공조달 매출 실적 | 업체 단위 조회 파라미터가 없다 — 기간 전수 스캔 후 `bidwinnrBizno` 로 거른다 |
| 나라장터 **계약** (`ao/CntrctInfoService`) | 계약 규모·거래 지속성 | 경로 접두사가 서비스마다 다르다(`as/` vs `ao/`). 틀리면 `NO_OPENAPI_SERVICE_ERROR` |
| 국민연금 사업장 | 고용 규모·인건비 | 사업자번호를 앞 6자리만 준다. 사업장 단위라 법인 합산 필요 |

- 배치 수집 전제이므로 `scripts/collect-financial-signals.ts` 의 방식을 따른다
- 지표로 올릴 때는 **재무제표 대체가 아니라 대리지표**임을 화면에 명시한다 — 근거의 급이 다르다

### 1-2. Task 16 배포 잔여

- **이미지 빌드·compose 기동 검증** — 이 저장소를 만든 환경에 Docker 데몬이 없어 미검증이다.
  `deploy/README.md` 에 그렇게 적어 뒀다. Docker 가 있는 곳에서 `docker compose -f deploy/docker-compose.prod.yml build` 부터 확인할 것.
  특히 `better-sqlite3` 는 네이티브 모듈이라 빌드·실행 이미지를 같은 베이스(`node:24-slim`)로 맞춰 뒀으나 실제 빌드로 확인해야 한다
- **E2E 수동 확인** — 로그인 → 분석 1회 → 리포트 다운로드. 위 빌드 검증 뒤에

---

## 2. 결정이 있어야 움직이는 것

### 2-1. SQLite → PostgreSQL

지금은 SQLite 다. 옮기려면 코드 변경이 따른다. 비용을 보고 판단할 것 — 절차는 `deploy/README.md` 하단에 적어 뒀다.

1. `prisma/schema.prisma` 의 `provider` 교체
2. 어댑터를 `@prisma/adapter-better-sqlite3` → `@prisma/adapter-pg` (`src/lib/db.ts`)
3. **기존 마이그레이션은 SQLite 방언이라 재사용할 수 없다** — 전부 재생성
4. 테스트가 `file:./prisma/test.db` 를 쓰므로 테스트용 PG 인스턴스도 필요
5. compose 에 `postgres` 서비스·healthcheck 추가

### 2-2. `.env.example` 이 git 에 없다

`.gitignore` 의 `.env*` 패턴에 걸려 추적되지 않는다. 로드맵은 "키 이름을 유지"하라고 적고 있어 의도와 어긋난다.
값이 전부 비어 있어 커밋해도 안전하다 — `!.env.example` 예외를 넣을지 정할 것.

### 2-3. 화면 문구 잔여

2026-09-01 에 해설성 문구 14곳을 걷어냈다. 판단이 갈릴 수 있는 것 둘을 남겼다.

- 히트맵의 `"실시간 · 미확정"` — 확정되지 않은 값을 확정으로 읽지 않게 하는 표시라 남겼다
- 대시보드 절 제목의 `"시상 후보 검토용"` · `"리스크 검토용"` — 3어절 용도 라벨

---

## 3. 사람이 해야 하는 것 (코드로 대신할 수 없음)

- **미확인 경보·주의 사건 26건 확인 처리.** 확인된 것만 랭킹 감점에 반영되므로, 지금 전 기업의 리스크 감점이 0인 이유이기도 하다. 기업 상세의 "확인 필요" 블록에서 처리한다

---

## 4. 다시 파헤치지 말 것 (이미 결론 난 것)

- **이퀄라이저(52)의 `no_news` 는 정답이다.** 질의 7종·웹 검색 2회로 실제 보도가 없음을 확인했다. 별칭을 넣으면 오탐만 늘어난다
- **dartlab 은 채택하지 않는다.** 진입점이 `Company(stockCode)` 라 상장사 전용이고, 56개사 중 종목코드 보유는 4개사이며 그 넷은 이미 재무가 있던 기업이다. 비율은 `financeRatios.ts` 로 낸다
- **딥리서치(구 Task 15·15b)는 폐기했다.** 복원이 필요하면 `git log -S "STORM"`
- **Python 사이드카는 걷어냈다.** 위 둘이 사라져 소비자가 0이 됐다. 복원은 `git log -S "FastAPI"`
- **금융위 기업재무정보(15043459)** 는 원천이 전자공시라 DART 와 같은 결측이 난다

---

## 5. 이번 세션(2026-09-01)에 끝난 것 — 요약

커밋 32개. 자세한 것은 `git log 1c9640c..HEAD`.

- **재무**: 감사보고서 원문(`document.xml`) 파싱으로 재무 확보 3→13개사. 부채·자본 총계까지 읽어 부채비율·ROE·영업이익률을 자체 계산(`financeRatios.ts`). 자본잠식 4개사는 숫자 대신 "자본잠식" 으로 적는다 — 그대로 나누면 순손실 497억인 기업의 ROE 가 +64% 로 찍힌다
- **수집·사건**: 구글 프록시 링크를 원문 URL 로 복원해 제공자 간 중복 제거 · 같은 사건의 중복 기사를 근거로 접어 개별 건수로 세지 않음(7일·Dice 0.4) · 별칭은 검색어일 뿐 관련성은 회사명으로 판정
- **정확성 7건**: 라우트 파라미터 검증 · 배치 busy 경합 · no_news 스테퍼 · **검증 실패 ≠ 검토 필요**(judge 가 못 돈 것을 판정으로 읽던 문제) · 연금 페이지네이션 · DART 상장정보 · 실행 이력 100건 상한
- **표기 정확성**: 하드코딩 전수 조사 후 7건 수정(12개월 라벨 · 후보 1건 · 감성 0 · 엑셀/화면 판정 불일치 등)
- **LLM 공급자 추상화**: `LLM_PROVIDER` 로 Anthropic↔OpenAI 전환. 기본 모델 `gpt-5.6-terra`
- **UI**: 기업 상세 기본정보를 표로 · 재무를 행 단위 표로 · 라이트 기본 + 헤더 테마 토글 · 해설성 문구 14곳 제거
